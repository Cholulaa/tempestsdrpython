# Core runtime dependency (the only hard requirement)
numpy>=1.19

# Optional (install as needed):
#   pillow      -> image loading / PNG output / GIF export   (pip install pillow)
#   flask       -> web control panel and fleet control server (pip install flask)
#   matplotlib  -> lightweight matplotlib live viewer         (pip install matplotlib)
#   pyrtlsdr    -> live RTL-SDR capture                       (pip install "pyrtlsdr<0.3")
#   SoapySDR    -> live capture for many other front-ends     (system package)
